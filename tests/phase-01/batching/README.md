# Batching Tests

Batch-dimension support, dynamic padding, `<pad>` masking, ignored padding targets, custom collate functions, DataLoader behavior, and real batched training experiments from Phase 01.
